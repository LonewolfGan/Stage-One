Nettoyage complet des mocks et connexion aux vraies API.
Ne toucher à aucun style, aucun composant visuel.
Uniquement remplacer les données fictives par de vrais appels API.
Travailler page par page dans l'ordre donné.

---
PAGE 1 — HOME

Problème : MARQUEE_ITEMS, COLLAGE_CARDS, BEAUTY_CASES, STEPS,
TESTIMONIALS, TESTIMONIALS_2 sont tous hardcodés.
Les catégories et providers ne sont pas fetchés depuis l'API.

Actions :

1. Catégories — fetcher depuis l'API :
   const { data: categories } = useQuery({
     queryKey: ['categories'],
     queryFn: () => api.get('/providers/categories')
   })
   Côté API, crée GET /api/providers/categories :
     SELECT DISTINCT category, COUNT(*) as count
     FROM providers WHERE status = 'ACTIVE'
     GROUP BY category
     Retourner : [{ slug, label, count }]

2. Providers "populaires" — fetcher les mieux notés :
   const { data: featured } = useQuery({
     queryKey: ['providers', 'featured'],
     queryFn: () => api.get('/providers?limit=6&sort=rating')
   })
   Côté API, ajoute ?sort=rating sur GET /api/providers :
     ORDER BY rating DESC LIMIT 6

3. MARQUEE_ITEMS — remplacer par les noms réels des providers
   depuis la même query featured.

4. TESTIMONIALS — fetcher les derniers avis 5 étoiles :
   GET /api/reviews/featured?limit=6
   Côté API :
     SELECT r.comment, r.rating, u.name as clientName,
            p.name as providerName
     FROM reviews r
     JOIN users u ON r.client_id = u.id
     JOIN providers p ON r.provider_id = p.id
     WHERE r.rating = 5
     ORDER BY r.created_at DESC
     LIMIT 6
   Si moins de 6 avis en base, afficher ceux disponibles
   sans fallback inventé.

5. STEPS, COLLAGE_CARDS, BEAUTY_CASES — ces éléments sont
   du contenu marketing statique (pas des données métier).
   Les laisser hardcodés est acceptable.
   Vérifier juste qu'ils n'affichent pas de vrais noms
   ou prix inventés — uniquement du texte générique.

---
PAGE 2 — SEARCH

Problème : MOROCCO_CITIES et CATS hardcodés,
coordonnées Maroc hardcodées.

Actions :

1. MOROCCO_CITIES — remplacer par un fetch :
   GET /api/providers/cities
   Côté API :
     SELECT DISTINCT city, COUNT(*) as providerCount
     FROM providers WHERE status = 'ACTIVE'
     GROUP BY city ORDER BY city ASC
   Si la liste est vide → garder un fallback minimal
   ['Casablanca', 'Rabat', 'Marrakech'] uniquement
   comme placeholder de champ, pas comme filtre actif.

2. CATS (catégories de filtres) — utiliser la même query
   que la home : GET /api/providers/categories
   const { data: cats } = useQuery({
     queryKey: ['categories'],
     queryFn: () => api.get('/providers/categories')
   })

3. Coordonnées Maroc hardcodées — acceptable pour
   le centre de carte par défaut. Garder tel quel.

4. Vérifier que api.searchProviders() passe bien
   les filtres actifs (ville, catégorie, type) en query params
   et que l'API les applique tous correctement.

---
PAGE 3 — PROVIDER PROFILE

Problème : auteur d'avis toujours "Client vérifié",
photos fallback Unsplash hardcodées.

Actions :

1. Auteur d'avis — l'API doit retourner le vrai prénom :
   Dans GET /api/providers/:slug :
     JOIN users ON reviews.client_id = users.id
     Retourner : review.clientName = user.name
   Dans le composant :
     Remplacer "Client vérifié" par {review.clientName}
     Si clientName est null → afficher "Client anonyme"
     (jamais "Client vérifié" comme valeur inventée)

2. Photos fallback Unsplash hardcodées — remplacer par :
   {provider.logoUrl ? (
     <img src={provider.logoUrl} />
   ) : (
     <div className="bg-[#F5F5F5] flex items-center justify-center">
       <ScissorsIcon className="w-8 h-8 text-[#C4C4C4]" />
     </div>
   )}
   Supprimer toutes les URLs Unsplash hardcodées
   dans ce composant.

3. Partage non tracké — laisser tel quel pour le MVP.
   Ce n'est pas dans le PRD.

---
PAGE 4 — BOOKING

Problème : dates générées localement, flag isMock,
bannière "Mode développement", bouton confirmMockMutation.

Actions :

1. Les 7 prochaines dates générées localement — c'est correct,
   les dates sont calculées côté client (pas besoin d'API).
   Garder tel quel.

2. Supprimer la bannière "Mode développement — paiement simulé"
   de l'UI. Remplacer par un composant neutre :

   function PaymentSection({ clientSecret, bookingId }) {
     const stripeKey = import.meta.env.VITE_STRIPE_PUBLISHABLE_KEY

     if (!stripeKey || stripeKey.includes('placeholder')) {
       return (
         <div className="border border-[rgba(0,0,0,0.08)]
                         rounded-[12px] p-6 text-center">
           <p className="text-[14px] text-[#525252] mb-4">
             Confirmer votre réservation
           </p>
           <button
             onClick={() => confirmMockMutation.mutate()}
             className="w-full h-11 bg-[#D4547A] text-white
                        text-[14px] font-medium rounded-[8px]
                        hover:bg-[#C04268] transition-colors"
           >
             Confirmer
           </button>
         </div>
       )
     }

     return (
       <Elements stripe={stripePromise} options={{ clientSecret }}>
         <StripePaymentForm bookingId={bookingId} />
       </Elements>
     )
   }

3. Le flag isMock dans l'API — garder le graceful fallback
   mais supprimer tout texte "mock" ou "dev" visible
   dans l'UI publique.

4. confirmMockMutation doit appeler la vraie route :
   POST /api/bookings/:id/confirm-mock
   Côté API :
     UPDATE bookings SET status = 'CONFIRMED',
     payment_status = 'simulated'
     WHERE id = bookingId AND status = 'PENDING'
     Émettre booking.confirmed via Socket.io
   Rediriger vers /booking/confirmation?id=bookingId

---
PAGE 5 — AGENDA DASHBOARD

Problème : fillRate hardcodé à 73%, noms "Client" / "Prestation"
si API vide, CA calculé localement.

Actions :

1. fillRate fallback 73% — remplacer par null :
   const fillRate = data?.fillRate ?? null
   Dans l'UI :
   {fillRate !== null ? `${fillRate}%` : '—'}
   Jamais afficher une valeur inventée.

2. Noms "Client" / "Prestation" si API vide — l'API doit
   retourner ces données. Vérifier que GET /api/dashboard/bookings
   fait bien les JOIN nécessaires :
     JOIN services ON bookings.service_id = services.id
     JOIN users ON bookings.client_id = users.id
   Retourner dans chaque booking :
     { serviceName, clientName, startDatetime, endDatetime, status }
   Dans le composant BookingBlock :
     {booking.serviceName || 'Prestation'}  → supprimer le fallback
     {booking.clientName || 'Client'}       → supprimer le fallback
   Si l'API retourne null c'est un bug backend à corriger.

3. BLOCK_DURATIONS hardcodé — [30, 60, 90, 120] minutes.
   C'est du contenu UI statique, pas des données métier.
   Garder tel quel.

4. CA calculé localement — déplacer ce calcul côté API :
   Dans GET /api/dashboard/analytics, ajoute dailyRevenue :
     SELECT DATE(start_datetime) as date,
            SUM(amount_cents) / 100.0 as revenue
     FROM bookings
     WHERE provider_id = $providerId
     AND status = 'CONFIRMED'
     AND start_datetime >= NOW() - INTERVAL '30 days'
     GROUP BY DATE(start_datetime)
   Supprimer le calcul local dans le frontend.

---
PAGE 6 — ANALYTICS DASHBOARD

Problème : MOCK_WEEK, MOCK_7D, MOCK_MONTH, MOCK_3M, MOCK_1Y,
MOCK_SERVICES, MOCK_STAFF_PERF tous générés avec Math.sin
et Math.random(). Fallbacks hardcodés 107/32400/73/84.

C'est la page avec le plus de mocks. Tout doit être branché.

Actions :

1. Étends GET /api/dashboard/analytics pour supporter
   les périodes :
   GET /api/dashboard/analytics?period=7d|30d|3m|1y

   Côté API, selon la période :
     7d  → GROUP BY DATE, derniers 7 jours
     30d → GROUP BY DATE, derniers 30 jours
     3m  → GROUP BY WEEK, derniers 3 mois
     1y  → GROUP BY MONTH, dernière année

   Structure de réponse complète :
   {
     period: '30d',
     totalBookings: number,        // COUNT(*)
     revenueMad: number,           // SUM(amount_cents)/100
     fillRate: number,             // bookings / max_possible * 100
     uniqueClients: number,        // COUNT(DISTINCT client_id)
     bookingsByPeriod: [           // pour le graphique
       { date: string, count: number, revenue: number }
     ],
     topServices: [                // TOP 5
       { serviceName: string, count: number, revenue: number }
     ],
     staffPerformance: [           // par staff
       { staffName: string, bookings: number, revenue: number }
     ]
   }

2. La requête staffPerformance :
   SELECT s.name as staffName,
          COUNT(b.id) as bookings,
          SUM(b.amount_cents) / 100.0 as revenue
   FROM bookings b
   JOIN staff s ON b.staff_id = s.id
   WHERE b.provider_id = $providerId
   AND b.status = 'CONFIRMED'
   AND b.start_datetime >= NOW() - $interval
   GROUP BY s.id, s.name
   ORDER BY revenue DESC

3. Dans le frontend — remplacer toutes les variables MOCK_* :
   const { data: analytics } = useQuery({
     queryKey: ['analytics', providerId, period],
     queryFn: () => api.get(`/dashboard/analytics?period=${period}`)
   })

   Supprimer : MOCK_WEEK, MOCK_7D, MOCK_MONTH,
               MOCK_3M, MOCK_1Y, MOCK_SERVICES, MOCK_STAFF_PERF
   Supprimer : les fallbacks hardcodés 107/32400/73/84

   Remplacer par :
   const totalBookings = analytics?.totalBookings ?? '—'
   const revenueMad = analytics?.revenueMad ?? '—'
   const fillRate = analytics?.fillRate ?? '—'
   const uniqueClients = analytics?.uniqueClients ?? '—'

4. Si la DB n'a pas encore assez de données pour 3m/1y,
   l'API retourne un tableau vide — le graphique affiche
   "Pas encore de données sur cette période"
   plutôt qu'une courbe Math.sin inventée.

   Empty state pour le graphique :
   {bookingsByPeriod.length === 0 ? (
     <div className="flex items-center justify-center h-48">
       <p className="text-[13px] text-[#8C8C8C]">
         Pas encore de données sur cette période
       </p>
     </div>
   ) : (
     <BarChart data={bookingsByPeriod} />
   )}

---
PAGE 7 — SETTINGS DASHBOARD

Problème : MOCK_PHOTOS (6 URLs Unsplash), profil par défaut
"Salon Atlas / Marrakech", sauvegarde horaires cassée,
upload photos désactivé "Phase 2".

Actions :

1. Profil par défaut "Salon Atlas / Marrakech" — fetcher
   depuis l'API :
   const { data: provider } = useQuery({
     queryKey: ['provider', providerId],
     queryFn: () => api.get(`/dashboard/provider`)
   })
   GET /api/dashboard/provider :
     SELECT * FROM providers WHERE owner_id = req.userId
   Supprimer le profil hardcodé.

2. MOCK_PHOTOS — remplacer par les vraies photos du provider :
   const photos = provider?.photos ?? []
   Si photos est vide → afficher une grille vide avec
   un message "Aucune photo ajoutée"
   Supprimer les 6 URLs Unsplash hardcodées.

3. Sauvegarde horaires cassée — l'API GET/PUT
   /api/dashboard/business-hours doit maintenant exister
   (implémentée dans le prompt précédent).
   Connecter le formulaire :
   const mutation = useMutation({
     mutationFn: (hours) =>
       api.put('/dashboard/business-hours', { hours }),
     onSuccess: () => toast.success('Horaires sauvegardés'),
     onError: () => toast.error('Erreur lors de la sauvegarde')
   })
   Supprimer le message "Sauvegarde non disponible en mode démo".

4. Upload photos "Phase 2" — deux options :
   Option A (recommandée pour MVP) : garder le bouton visible
   mais afficher un message honnête :
   "Upload de photos disponible après configuration
    du stockage Cloudflare R2"
   Pas de "Phase 2" — juste une explication claire.

   Option B : implémenter l'upload maintenant avec
   Cloudflare R2 si CLOUDFLARE_R2_* est configuré,
   sinon désactiver proprement.
   Choisir Option A pour le MVP.

---
PAGE 8 — SUBSCRIPTION DASHBOARD

Problème : PLANS hardcodés avec prix, footer "Mode démo",
Stripe commenté ligne 210.

Actions :

1. PLANS hardcodés — les plans et leurs features sont
   du contenu statique, pas des données DB.
   Garder les définitions hardcodées dans le frontend.
   C'est correct.

2. Ce qui doit venir de l'API :
   - Le plan actuel du provider
   - La date de renouvellement

   const { data: subscription } = useQuery({
     queryKey: ['subscription', providerId],
     queryFn: () => api.get('/dashboard/subscription')
   })
   const currentPlan = subscription?.plan ?? 'FREE'

3. Supprimer le footer "Mode démo".
   Remplacer par :
   <p className="text-[12px] text-[#8C8C8C] text-center mt-4">
     Le changement de plan sera effectif immédiatement.
     Contactez-nous pour toute question.
   </p>

4. onError → supprimer "Changement de plan non disponible
   en mode démo". Remplacer par :
   onError: () => toast.error('Erreur lors du changement de plan.
     Veuillez réessayer.')

5. Stripe ligne 210 — laisser commenté pour l'instant.
   L'intégration Stripe pour les abonnements se fait
   quand les credentials sont configurés.
   Ne pas décommenter maintenant.

---
VÉRIFICATION FINALE

Après chaque page, vérifie :

  [ ] Aucune variable MOCK_* dans le fichier
  [ ] Aucun Math.random() ou Math.sin() pour des données UI
  [ ] Aucun texte "Phase 2", "Mode démo", "non disponible
      en mode démo" visible dans l'UI publique
  [ ] Aucune URL Unsplash hardcodée dans les composants
      (autorisées uniquement dans seed.ts)
  [ ] Tous les fallbacks affichent '—' ou un empty state
      propre, jamais une valeur inventée
  [ ] npx tsc --noEmit passe sans erreur

---
ORDRE D'EXÉCUTION

1. Analytics (le plus de mocks, le plus critique pour la démo)
2. Agenda (fillRate + noms clients/prestations)
3. Settings (horaires + profil)
4. Home (catégories + featured providers)
5. Search (villes + catégories depuis API)
6. Booking (supprimer bannière dev)
7. Provider Profile (noms auteurs avis)
8. Subscription (plan actuel depuis API)