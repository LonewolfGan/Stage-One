- [PSTAGEV1 Phase 1 build](pstagev1-phase1.md) — Full Awwwards redesign complete; accent #D4466E; frontend on port 5000 BASE_PATH=/.
- [Button hierarchy rule](button-hierarchy-rule.md) — Primary CTA (confirm, reserve, search, save, login) = rose var(--accent); secondary = dark/outline. DSButton variant="primary" now uses var(--accent) in index.css.
- [API Server workflow quirks](api-server-workflow.md) — Use port 8000 (not 8080), pre-build binary in bash then start with node directly; Vite proxy /api → localhost:8000.
- [UI component casing convention](ui-casing-convention.md) — Custom DS components use PascalCase (Badge.tsx, DSButton.tsx, Input.tsx); shadcn uses lowercase (button.tsx, input.tsx). Never import Badge/Button across the casing boundary.
- [Mock data migration](mock-data-migration.md) — mock-data.ts deleted; all pages use real API via useQuery + adaptProvider/adaptProviderList; dashboard 401s are expected (auth required).
- [Dev seeding & auto-login](dev-seeding.md) — Salon Atlas seeded: provider-atlas / owner-atlas / 3 staff / 5 services / 20 bookings. DevAutoLogin component in App.tsx auto-fetches JWT on first load (dev only). Owner creds: owner@salonatlas.ma / atlas2026. Insert timestamps in UTC (no +01 offset).
- [Phase 2 implementation](phase2-implementation.md) — 7 priorities implemented: Stripe (code+mock fallback), socket.io-client hooked, GIST+SELECT FOR UPDATE+Redis lock, Firebase auth code, agenda click-handler modal, /account/bookings page, helmet+rate-limit+CORS. Needs env vars: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, VITE_STRIPE_PUBLISHABLE_KEY, REDIS_URL, FIREBASE_PROJECT_ID/KEY/EMAIL.
- [Notifications system](notifications-system.md) — `notifications` table in DB; 3 routes on /dashboard/notifications; booking confirm saves notif; DashboardLayout has bell+badge+dropdown panel. ~97% PRD coverage; only PostGIS geospatial (R-FUNC-033) remains for Phase 3.
- [Design system color audit](design-system-color-audit.md) — All status colors now go through ds.colors. PENDING=accent(#D4466E), CONFIRMED=success(#33CA7F), CANCELLED=error(#E11D32). DS gained info/infoBg/infoBorder. Remaining hex literals are intentional: white-on-dark buttons (#fff on ink/accent), data-viz categoricals in analytics/search.
- [Design redesign plan](design-redesign-plan.md) — Full plan for component redesign session: ScrollX UI + shadcn MCP ready, audit order, priority components, rules. Read `.local/design-redesign-plan.md` to start.
